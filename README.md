# Express & MongoDB Dockerized Application

Практична робота з Docker, Express та MongoDB. Проект демонструє контейнеризацію Node.js додатка з інтеграцією MongoDB за допомогою Docker Compose.

## 📌 Коротко про проект

- Express сервер з маршрутами / та /status
- Підключення до MongoDB через MONGODB_URI
- Dockerfile на базі node:lts
- Docker Compose для app + mongo
- Hot reload через nodemon та volumes

## 📂 Структура проекту

```
home-work-68-express-9/
├── app.js
├── Dockerfile
├── docker-compose.yml
├── nodemon.json
├── package.json
├── .gitignore
├── .dockerignore
└── README.md
```

## ✅ Реалізовані маршрути

| Метод | Шлях    | Опис                  |
| ----- | ------- | --------------------- |
| GET   | /       | Вітальне повідомлення |
| GET   | /status | Статус сервера і БД   |

Приклад відповіді /status:

```
{
  "status": "Server is up",
  "database": "connected"
}
```

## 🚀 Запуск через Docker Compose

1. Клонування репозиторію

```
git clone <your-repository-url>
cd home-work-68-express-9
```

2. Збірка та запуск

```
docker-compose up --build
```

3. Перевірка у браузері

- http://localhost:3000
- http://localhost:3000/status

4. Зупинка контейнерів

```
docker-compose down
```

## 🔄 Hot Reload (автооновлення)

1. Запустіть контейнери (docker-compose up)
2. Відкрийте app.js у редакторі
3. Змініть текст у res.send(...)
4. Збережіть файл
5. Оновіть сторінку — зміни застосуються автоматично

Пояснення:

- volumes синхронізують локальні файли з контейнером
- nodemon відстежує зміни й перезапускає сервер

## 🧩 Технічні деталі

Dockerfile:

- Базовий образ: node:lts
- Робоча директорія: /app
- Порт: 3000
- Команда запуску: node app.js

Docker Compose:

- app — Express сервіс
- mongo — MongoDB сервіс
- volumes:
  - .:/app
  - /app/node_modules
  - mongo_data:/data/db

Змінні середовища:

- MONGODB_URI=mongodb://mongo:27017/appdb

## 🧪 Тестування

1. Сервер доступний на http://localhost:3000
2. Статус БД: http://localhost:3000/status
3. Зміни в app.js застосовуються без перезапуску

## 📊 Корисні команди

```
docker ps
docker-compose logs app
docker-compose logs mongo
docker-compose logs -f
docker-compose restart app
docker-compose down -v
```

## 🐛 Troubleshooting

Проблема: Порт 3000 зайнятий
Рішення: змініть порт у docker-compose.yml, наприклад 3001:3000

Проблема: app завершується з помилкою
Рішення: docker-compose logs app

Проблема: MongoDB не підключається
Рішення: переконайтеся, що обидва контейнери запущені (docker ps)

## 📝 Критерії виконання

✅ Express додаток з маршрутами / та /status
✅ Dockerfile на node:lts
✅ Docker Compose з app + mongo
✅ MONGODB_URI налаштовано
✅ Volumes для hot reload
✅ Додаток доступний на localhost:3000
